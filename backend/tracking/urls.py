from django.urls import path
from . import views

urlpatterns = [
    # path('history/', views.DoseHistoryView.as_view(), name='dose_history'),
    # path('adherence/', views.AdherenceView.as_view(), name='adherence'),
    # path('report/weekly/', views.WeeklyReportView.as_view(), name='weekly_report'),
]